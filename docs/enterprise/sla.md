# Support SLA and status (P6)

Pilot support SLO: weekly schedules, digest delivery, and deletion completion
records. Formal enterprise SLAs, incident commitments, and procurement package
are issued per contract. Status: public `/api/health` and `/api/ready`;
incidents follow `docs/runbooks`. Additional VCS systems and language
extractors ship on contracted demand.
