# Data processing addendum (pilot)

The customer is the controller of repository and source content. Nexus Forge acts
as processor, processing only on documented instructions (admission manifests),
with tenant-scoped access controls, sealed deterministic ledgers, and
cryptographic erasure via tenant-key destruction on workspace deletion.
Subprocessors are limited to hosting, database, and the configured model provider.
