# Subprocessors (pilot)

- Hosting and database provider (as configured by deployment).
- Groq (model inference, only when external inference is explicitly authorized).
- GitHub (repository collection via the connected GitHub App).

No other provider receives customer content. Deterministic-only runs contact no
model provider.
